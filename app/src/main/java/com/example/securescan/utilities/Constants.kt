package com.example.securescan.utilities

object Constants {

    object Navigation {
        const val SUCCESS_AUTO_NAV_DELAY_MS = 2_000L
    }

    object DateFormats {
        const val SERVER_ISO_UTC = "yyyy-MM-dd'T'HH:mm:ssX"
        const val UI_READABLE = "MMM dd, yyyy"
        const val UTC_TIMEZONE_ID = "UTC"
    }

    object DeviceInfo {
        const val OS_NAME = "Android"
        const val VERSION_UNAVAILABLE = "Version unavailable"
    }

    object Intents {
        const val PACKAGE_SCHEME = "package"
    }
}
